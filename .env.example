# Root .env is intentionally minimal — the meaningful env lives in backend/.env
# (referenced by docker-compose `env_file`). Override TZ here only if you need
# the host shell to know it.
TZ=America/Argentina/Buenos_Aires
